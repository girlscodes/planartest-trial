export declare class User {
  region: number;
  player_id: number;
  sex?: number;
  education?: string;
  birthYear?: number;
  constructor(region: number, player_id: number, sex?: number, education?: string, birthYear?: number);
}
